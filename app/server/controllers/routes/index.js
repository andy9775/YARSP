/**
 * Export configured server endpoints to serve HTML and api requests
 */

import * as api from 'controllers/routes/api';
import { frontEnd } from 'controllers/routes/frontend';

export { api, frontEnd };
